import React from "react";

export default function VendorFlow({
  token,
  meta,
}: {
  token: string;
  meta: any;
}) {
  return (
    <div className="mt-6 text-sm text-zinc-700">
      Vendor Onboarding Flow (stub)
    </div>
  );
}
