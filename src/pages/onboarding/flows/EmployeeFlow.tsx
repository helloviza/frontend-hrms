import React from "react";

export default function EmployeeFlow({
  token,
  meta,
}: {
  token: string;
  meta: any;
}) {
  return (
    <div className="mt-6 text-sm text-zinc-700">
      Employee Onboarding Flow (stub)
    </div>
  );
}
